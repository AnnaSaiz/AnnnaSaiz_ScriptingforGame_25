using UnityEngine;

public class LoseorWin : MonoBehaviour
{
    public CountDownTimer isIt;
    public keepCount howMuch;
    public SpawnIn doesItMatch;
    public GameObject winScreen;
    public GameObject loseScreen;


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // howMuch = GetComponent<currentCount>();
        //doesItMatch = GetComponent<
    }

    // Update is called once per frame
    void Update()
    {
       // if(isIt.timeRemaining(). != true)
        {
            //need  to check if count matches number of wests 
            //if(howMuch !=
        }
        
           
    }
}
