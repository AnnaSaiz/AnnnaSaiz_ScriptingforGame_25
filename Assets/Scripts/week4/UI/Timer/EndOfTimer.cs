using UnityEngine;

public class EndOfTimer : MonoBehaviour

{
    /*
    public CountDownTimer timerEnd;
    public keepCount countEnd;
    public bool isRunning;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
      
    }

    // Update is called once per frame
    void Update()
    {
          if(timerEnd != null)
          {
            float timeRemaining = timerEnd.GetTimeRemaining();

           if (timeRemaining > 0)
           {

             isRunning = true;
           }
        
           else if(timeRemaining <= 0)
           {
             isRunning = false;
             countEnd.enabled = false;

            }
          }
    }
    */
}
